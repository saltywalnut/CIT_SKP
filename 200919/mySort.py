list = [3,72,85,49,1,65,23,25]

def sortlist (list):
    (list[y]) = length
    (list[x]) = 1
    length = len(list)
    while length > 0:
        if (list[x]) > (list[y]):
            list2.append (list[x])
            print (list[x])
        else:
            list2.append (list[y])
            print (list[y])
        length -= 1
